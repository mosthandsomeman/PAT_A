//#include<cstdio>
//#include<cstdlib>
//#include<string>
//#include<algorithm>
//#include<vector>
//#include<iostream>
//using namespace std;
//int n, c;
//struct Node
//{
//	int id, score;
//	string name;
//}node;
//vector<Node> vec;
//
//bool cmp(Node & a, Node & b) {
//	if (c == 1) {
//		if (a.id != b.id) return a.id < b.id;
//	}
//	else if (c == 2) {
//		if (a.name != b.name) return a.name < b.name;
//		else return a.id < b.id;
//	}
//	else {
//		if (a.score != b.score) return a.score < b.score;
//		else return a.id < b.id;
//	}
//}
//
//int main() {
//	cin >> n >> c;
//	for (int i = 0;i < n;++i) {
//		cin >> node.id >> node.name >> node.score;
//		vec.push_back(node);
//	}
//	sort(vec.begin(), vec.end(), cmp);
//	for (int i = 0;i < vec.size();++i) {
//		printf("%06d %s %d\n", vec[i].id, vec[i].name.c_str(), vec[i].score);
//	}
//	system("pause");
//	return 0;
//}
